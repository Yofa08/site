# Normalize line endings to LF everywhere (repository AND working tree).
# Deploy target is Linux (Railway); all source files are already LF.
# This overrides any core.autocrlf=true set in Windows Git, so files
# stop being flagged "will convert to CRLF on checkout".
* text=auto eol=lf
